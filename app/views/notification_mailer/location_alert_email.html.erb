<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Alert Data for <%= @location_name %></title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background: linear-gradient(135deg, #e63946 0%, #f4a261 100%);
      color: white;
      padding: 30px;
      text-align: center;
      border-radius: 8px 8px 0 0;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
    }
    .header .type {
      font-size: 12px;
      text-transform: uppercase;
      opacity: 0.9;
      margin-top: 5px;
    }
    .alert-icon {
      font-size: 36px;
      margin-bottom: 10px;
    }
    .content {
      background: #fff9f0;
      padding: 30px;
      border: 1px solid #f4a261;
      border-top: none;
    }
    .alert-meta {
      background: #fff;
      padding: 15px;
      border-radius: 4px;
      margin: 20px 0;
      border-left: 4px solid #e63946;
    }
    .alert-meta strong {
      color: #e63946;
    }
    .count-highlight {
      font-size: 32px;
      font-weight: bold;
      color: #e63946;
    }
    .footer {
      background: #333;
      color: #999;
      padding: 20px;
      text-align: center;
      font-size: 12px;
      border-radius: 0 0 8px 8px;
    }
    .footer a {
      color: #f4a261;
      text-decoration: none;
    }
    .button {
      display: inline-block;
      background: #e63946;
      color: white;
      padding: 12px 24px;
      text-decoration: none;
      border-radius: 4px;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="alert-icon">🔔</div>
    <div class="type">Location Alert</div>
    <h1>New Data for <%= @location_name %></h1>
  </div>

  <div class="content">
    <p>Hi<%= @user.name.present? ? " #{@user.name}" : "" %>,</p>

    <p>New alert data has been detected for one of your saved locations:</p>

    <div class="alert-meta">
      <p><strong>Location:</strong> <%= @location_name %></p>
      <p><strong>New data period:</strong> <%= Date.parse(@latest_date).strftime('%B %Y') rescue @latest_date %></p>
      <% if @latest_count.present? %>
        <p><strong>Alert count:</strong> <span class="count-highlight"><%= number_with_delimiter(@latest_count) %></span></p>
      <% end %>
    </div>

    <% if @new_dates.length > 1 %>
      <p><em>Note: <%= @new_dates.length %> new data periods were added.</em></p>
    <% end %>

    <p>
      <a href="<%= ENV.fetch('FRONTEND_URL', 'https://globalmangrovewatch.org') %>" class="button">
        View on Global Mangrove Watch
      </a>
    </p>
  </div>

  <div class="footer">
    <p>
      You received this email because you have location alerts enabled for <%= @location_name %>.
    </p>
    <p>
      <a href="<%= ENV.fetch('FRONTEND_URL', 'https://globalmangrovewatch.org') %>/settings">
        Manage your saved locations and alert preferences
      </a>
    </p>
    <p>© <%= Time.current.year %> Global Mangrove Watch</p>
  </div>
</body>
</html>

