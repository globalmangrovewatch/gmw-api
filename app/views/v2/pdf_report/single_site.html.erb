<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>Site Report</title>
  </head>
  <body>
    <% @single_site.each do |site_row| %>
      <h1 class="font-heading">
        Site: <%= site_row['site_name'] %>
      </h1>
    <% end %>

    <!-- Start of Registration Answers -->
    <% @pdf_reg_answers.each do |category, questions| %>
      <h2><%= category %></h2>
      <% questions.each do |key, item| %>
        <hr class="no-margin">
        <div class= "<%= cycle('white', 'mint') -%> question-padding">
          <table>   
            <tr>
              <td class="td-padding"><%= key %></td>          
              <td><b><%= item[:name] %></b></td>
            </tr>
            <% item[:value].each do |x| %>
              <tr>
                <td></td>
                <td><%= x %></td>
              </tr>
            <% end %>
          </table>
        </div>
      <% end %>
      <div class="pagebreak"> </div>
    <% end %>

    <!-- Start of Monitoring Answers -->
    <% @pdf_mon_answers.each do |key, value| %>
      <% value["answers"].each do |x, y| %>
        <hr class="no-margin">
        <div class= "<%= cycle('white', 'mint') -%> question-padding">
          <table>         
            <tr>
              <td class="td-align"><%= x %></td>          
              <td><b><%= y[:name] %></b></td>
            </tr>
            <% y[:value].each do |item| %>
            <tr>
              <td></td>
              <td><%= item %></td>
            </tr>
            <% end %>
          </table>
        </div>
      <% end %>
    <% end %>    
  </body>
</html>
